/** @type {import('next').NextConfig} */
const nextConfig = {
  // Using Cloudflare's recommended settings
  // Build output will be at .vercel/output/static
  // Build command: npx @cloudflare/next-on-pages@1
}

module.exports = nextConfig
